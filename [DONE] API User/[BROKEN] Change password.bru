meta {
  name: [BROKEN] Change password
  type: http
  seq: 3
}

put {
  url: {{APP_DOMAIN}}/users/change-password
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  old_password: password
  new_password: newpassword
}

settings {
  encodeUrl: true
  timeout: 0
}
