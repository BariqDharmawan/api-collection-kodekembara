meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{APP_DOMAIN}}/auth/login
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  email: admin@kodekembara.com
  password: password
}

settings {
  encodeUrl: true
  timeout: 0
}
