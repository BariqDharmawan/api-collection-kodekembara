meta {
  name: Add new careers
  type: http
  seq: 2
}

post {
  url: {{APP_DOMAIN}}/careers
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: Frontend Devz
}

settings {
  encodeUrl: true
}
